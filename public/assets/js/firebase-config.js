// Minimal Firebase configuration holder.
// Replace the placeholder values below with your Firebase project's config.
// To obtain these values, go to your Firebase console → Project settings → SDK setup and configuration.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};

// Notes:
// - Do NOT commit real production credentials to a public repository.
// - For local development it's fine to paste the config here, or load it from an environment-aware build.
